# Dependencies
node_modules/

# Local dev database and data directory
.data/
data/
*.db
*.db-journal
*.sqlite

# Secrets — never commit these
.dev.vars
.env
.env.*
!.dev.vars.example

# Package lock (repo convention: dependencies are pinned by semver ranges)
package-lock.json

# Build output
dist/
.wrangler/

# Logs / os cruft
*.log
.DS_Store
